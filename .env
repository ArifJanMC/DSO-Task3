FLASK_CONFIG=development
SECRET_KEY=your-secret-key
# Explicitly set SQLite for testing
SQLALCHEMY_DATABASE_URI=sqlite:///:memory:
# Empty MySQL URI to prevent attempts to connect
MYSQL_URI=
# Empty PostgreSQL URI as we're using SQLite for now
POSTGRES_URI=
